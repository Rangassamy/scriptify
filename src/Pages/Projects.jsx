import React from "react";
import Navbar from "../Components/Navbar";

const Projects = () => {
  const page = 2;
  return (
    <div>
      <Navbar key={page} />
    </div>
  );
};

export default Projects;
